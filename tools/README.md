# Tools

This directory contains scripts and utilities to support the skills and prompts.

## Structure

- `git-sync/` - Tools for synchronizing with git repositories
- `validation/` - Validation and testing utilities

## Usage

Each subdirectory contains its own README with specific usage instructions.
