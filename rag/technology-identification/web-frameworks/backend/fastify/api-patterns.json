{
  "technology": "Fastify",
  "category": "web-frameworks/backend",
  "confidence": 60,
  "patterns": [
    {
      "type": "route-definitions",
      "code_patterns": [
        "fastify.get(",
        "fastify.post(",
        "fastify.route("
      ],
      "description": "Fastify route definitions"
    }
  ],
  "notes": [
    "Routes defined with .get(), .post(), etc.",
    "Can use .route() for multiple methods",
    "Supports JSON Schema validation"
  ]
}
