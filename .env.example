# Gibson Powers Configuration
# Copy this file to .env and add your API keys
#
# DO NOT commit .env file - it contains secrets!

#############################################################################
# API Keys (Required for Claude AI features)
#############################################################################

# Anthropic API Key for Claude integration
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# GitHub Personal Access Token for repository access
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo, read:org (for organization scanning)
GITHUB_TOKEN=your_github_token_here
GH_TOKEN=$GITHUB_TOKEN  # gh CLI also needs GH_TOKEN

#############################################################################
# RAG (Retrieval-Augmented Generation) Configuration
#############################################################################

# RAG Knowledge Base Directory
# Default: ./rag (relative to repository root)
# Set to absolute path if using external knowledge base
# RAG_DIR=/path/to/rag

# RAG Server Configuration (Optional - for production deployments)
# When set, uses semantic search via RAG server instead of local filesystem
# Supported servers: Pinecone, Weaviate, ChromaDB, Qdrant
# RAG_SERVER_URL=https://your-rag-server.example.com
# RAG_API_KEY=your_rag_server_api_key

# RAG Server Type (auto-detected from URL if not set)
# Options: pinecone, weaviate, chromadb, qdrant, local
# RAG_SERVER_TYPE=local

# RAG Collection/Index Name (for vector databases)
# RAG_COLLECTION=gibson-powers-knowledge

#############################################################################
# Claude Model Configuration
#############################################################################

# Override default Claude model
# CLAUDE_MODEL=claude-sonnet-4-20250514

# API timeout (seconds)
# API_TIMEOUT=30

# Max tokens for responses
# CLAUDE_MAX_TOKENS=4096

#############################################################################
# Output Configuration
#############################################################################

# Default output format: markdown, json, text
# DEFAULT_OUTPUT_FORMAT=markdown

# Enable cost tracking for Claude API usage
# ENABLE_COST_TRACKING=true
